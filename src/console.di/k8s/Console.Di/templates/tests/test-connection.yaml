apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Console.Di.fullname" . }}-test-connection"
  labels:
    {{- include "Console.Di.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Console.Di.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
